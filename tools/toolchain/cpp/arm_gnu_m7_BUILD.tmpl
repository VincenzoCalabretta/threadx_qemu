# (c) The Exploration Company
# Auto-generated by download_arm_gnu_m7_toolchain — do not edit.

load("@rules_cc//cc/toolchains:cc_toolchain.bzl", "cc_toolchain")
load(":{{toolchain_name}}_config.bzl", _arm_gnu_m7_toolchain_config = "cc_toolchain_config")

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "all_files",
    srcs = glob(
        [
            "bin/**",
            "arm-none-eabi/**",
            "lib/**",
            "libexec/**",
        ],
        allow_empty = False,
    ),
)

# =========================== Arm GNU M7 toolchain =========================== #

_arm_gnu_m7_toolchain_config(name = "{{toolchain_name}}_config")

cc_toolchain(
    name = "{{toolchain_name}}_cc",
    all_files = ":all_files",
    compiler_files = ":all_files",
    dwp_files = ":all_files",
    linker_files = ":all_files",
    objcopy_files = ":all_files",
    strip_files = ":all_files",
    supports_param_files = 0,
    toolchain_config = ":{{toolchain_name}}_config",
    toolchain_identifier = "{{toolchain_name}}-toolchain",
)

toolchain(
    name = "{{toolchain_name}}",
    exec_compatible_with = [
        "@platforms//os:linux",
        "@platforms//cpu:{{bazel_arch}}",
    ],
    target_compatible_with = [
        "@platforms//os:none",
        "@platforms//cpu:armv7e-m",
    ],
    toolchain = ":{{toolchain_name}}_cc",
    toolchain_type = "@bazel_tools//tools/cpp:toolchain_type",
)
